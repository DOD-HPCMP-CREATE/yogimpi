To get working before leaving:
 - Add MPI c2f and f2c functions.
 - Generate Fortran layer interface.
   - MPI_IN_PLACE, MPI_STATUS_IGNORE, MPI_STATUSES_IGNORE, MPI_BOTTOM, MPI_UNWEIGHTED, and MPI_WEIGHTS_EMPTY in Fortran?
   - ISO C bind these variables.
   - Revamp preprocessor for Fortran.

 - Ensure existing Fortran and C tests work with YogiMPI.
 - Build avtools and Kestrel with new YogiMPI.
 - Test that Kestrel still runs correctly.

Handle in MPI_Status:
 - Checking for MPI_PROC_NULL for source
 - Checking for MPI_ANY_TAG as tag

- MPI_Get_library_version string length stuff.

Possibly you need to convert errorcodes in some functions.
- Error codes for DATAREP stuff.

New constants:
 - MPI_MAX_DATAREP_STRING (must be at least 64)
 - MPI_MESSAGE_NO_PROC

 this is a function conv_onesided_constants

- Predefined operation constants 
  - MPI_REPLACE
  - MPI_NO_OP

- MPI_ARGVS_NULL as pointer
- MPI_ERR_SPAWN
- MPI_ERR_PORT

For arrays where conversions must happen, need to establish dimensions.
MPI_Comm_spawn and MPI_Comm_spawn_multiple are doom-a-licious.
MPI_Type_match_size does predefined stuff.
Do validate on MPI handles and check if they're MPI_BLAH_NULL or 
MPI_GROUP_EMPTY for input and output.
MPI_Type_create_darray
MPI_Type_get_envelope crap
Check functions that have errorcodes as arguments.
